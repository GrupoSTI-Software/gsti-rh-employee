#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Ejecutando linter..."
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ El linter encontró errores. Por favor corrígelos antes de hacer commit."
  exit 1
fi

echo "✅ Linter pasó correctamente"
